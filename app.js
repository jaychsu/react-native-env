import React from 'react'
import {
  Text,
  View,
} from 'react-native'

export default function () {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <Text>React Native Env</Text>
    </View>
  )
}
